import { GuideVoyage } from './guide-voyage';

describe('GuideVoyage', () => {
  it('should create an instance', () => {
    expect(new GuideVoyage()).toBeTruthy();
  });
});
function expect(arg0: GuideVoyage) {
  throw new Error('Function not implemented.');
}

